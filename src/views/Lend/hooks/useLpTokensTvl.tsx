/* eslint-disable no-unused-expressions */
import BigNumber from 'bignumber.js'
import { getTvl } from '../../Farms/helpers'

export const useGetlpTokensTvl = (farmsData) =>{

    // let lpTokensTvl = new BigNumber(0)
    // farmsData.map((farm) => {
    //   const { totalTvl } = getTvl(farm)
    //   lpTokensTvl = new BigNumber(totalTvl).plus(lpTokensTvl)
    //   return lpTokensTvl
    // })

    // return lpTokensTvl
    return 1
}